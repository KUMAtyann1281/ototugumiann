<%= form_with model: notice, url: admin_notices_path, local: true do |f| %>
  <div class="form-group">
    <%= f.label "お知らせタイトル" %><br/>
    <%= f.text_field :title, class: 'form-control notice_title' %>
  </div>
  <div class="form-group">
    <%= f.label "本文" %><br/>
    <%= f.text_area :body, class: 'form-control notice_body' %>
  </div>

  <div class="form-group row text-center">
    <% if request.fullpath.include?("edit") %>
      <div class="col-3 offset-3 text-center">
        <%= f.submit '更新', class: 'btn btn-success w-75' %>
      </div>
      <div class="col-3 text-center">
        <%= link_to "削除", admin_notices_path, method: :delete, data: { confirm: '本当に消しますか？' }, class: "btn btn-danger w-75" %>
      </div>
    <% else %>
      <div class="col-4 offset-4 text-center">
        <%= f.submit '投稿', class: 'btn btn-success w-50' %>
      </div>
    <% end %>
  </div>
<% end %>